import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["media.wired.com"], // Add your external image domains here
  },
};

export default nextConfig;
